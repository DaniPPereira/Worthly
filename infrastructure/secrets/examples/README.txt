Example secret files for local Compose. Replace before any real deployment.

These values are placeholders, not production credentials.
Never commit a real .env, PKCS#8 signing key, or production password file.
